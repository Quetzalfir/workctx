@AGENTS.md

# Gemini CLI project instructions

The canonical reusable workflows are under `.agents/skills/`, and the implementation plan is under `.agents/plan/initial/`.

All repository artifacts must be written in English. Communicate with the human operator in the interaction language configured by `.agents/operator.local.yaml` when present; otherwise use English.
