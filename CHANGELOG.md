# Changelog

All notable changes will be documented here.

## Unreleased

### Added

- Initial implementation scaffold.
- Multi-agent implementation plan and work-order contracts.
- Reference and retrieval model specification.
- CLI foundation and reusable context template.
- Wave 1: reference contracts and canonical URIs (WP-100), workspace schema and
  context template (WP-110), portable skill contract (WP-130), CLI envelope with
  exit bands and JSON output (WP-120).
- Wave 2: SQLite/FTS projections with typed queries (WP-210), validation engine
  and diagnostics (WP-220), canonical store with locking, staging, and atomic
  writes (WP-200), retrieval and context packs (WP-230), retrieval CLI wiring.
- Wave 3: staged filesystem extensions (WP-201), transaction engine with audit
  ledger (WP-300), stdio MCP server (WP-330), agent installers for Codex, Claude
  Code, and Gemini CLI (WP-320), inbox artifact lifecycle with quarantine and
  post-commit archive (WP-310), CLI wiring for the ready engines (LEAD-W1),
  per-client MCP configuration generation (LEAD-W2).
