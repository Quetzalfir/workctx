# Brief: DOCS-R1 — Documentation refresh to post-Wave-3 truth

Claude agent, own worktree. The single source of truth for what is stale is
`.agents/plan/2026-08-02_172559-phase1-consolidation/docs-claims.md` — fix EVERY item
there, in place, keeping each document's existing voice and structure.

## Scope

- README.md: current-status truthful (engines, MCP, installer, CLI groups shipped),
  lifecycle section hedged to match reality (inbox/archive live; drafting WP-420
  pending), contributor sync line gains `--extra mcp`, adapter file list matches
  docs/reference/agent-adapters.md, agent-open example marked as now-real
  (`workctx agent open`), MCP config generation now real (LEAD-W2).
- CHANGELOG.md: backfill Unreleased with Waves 1-3 entries (grouped by wave, terse,
  factual — source: `.agents/status/integration-log.md`).
- ROADMAP.md: per-item status markers (done/in-progress/pending) for Phase 1 bullets.
- docs/reference/{cli-envelope→SKIP (owned by another worker), canonical-store,
  projections, agent-adapters, skill-adapters}.md: fix the temporal WP-drift items
  listed in docs-claims.md; note in projections.md that incremental updates are
  explicitly post-alpha (unowned residue — say so).
- docs/concepts.md artifact claim; docs/guides/{evidence-processing (hedge steps to
  current truth: registration/quarantine/archive live, extraction rails landing in
  WP-410), quickstart (installer engine is real; `agent install` CLI now exists),
  multiple-contexts (plugin/secret claims → planned, Phase 3), context-layout
  (04_views → generated by WP-400, landing)}.

## Do NOT touch

`docs/reference/cli-envelope.md`, `docs/reference/mcp.md`,
`docs/reference/{transactions,validation-diagnostics,context-packs,views,
evidence-workflow,inbox}.md` (accurate or owned by running workers), any code, any
schema, `.agents/**` beyond reading, CLAUDE/GEMINI/AGENTS bridges.

## Acceptance

Every docs-claims.md line addressed (fixed or explicitly justified as already
correct); no NEW claims about unimplemented behavior; `grep -E "WP-[0-9]{3}"
docs/reference` returns only intentional references (release notes style, not
future-tense promises); English only.
