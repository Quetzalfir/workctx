# Worker assignment: `<WORK_ORDER_ID>`

You are implementing `<WORK_ORDER_ID>` for Work Context OS.

Read `AGENTS.md`, then read this work order's `contract.json`, `context.md`, and `acceptance.md`.

Work only inside the assigned worktree and allowed paths. Keep repository files in English and communicate with the human operator in the configured interaction language. Do not change architecture, scope, or forbidden paths without stopping and reporting the blocker.

Implement the contracted behavior, add tests, run every validation command, and write both `report.md` and `report.json` before declaring completion.
