"""Adapters around the domain core: filesystem, SQLite, agents, secrets, plugins.

File-level path ownership inside this package is assigned per work order; see
.agents/status/path-ownership.json. This module stays empty until the
implementation lead consolidates public re-exports at wave integration.
"""
